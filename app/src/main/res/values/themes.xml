<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.OpeninApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/light_blue</item>
    </style>

    <style name="Theme.OpeninApp" parent="Base.Theme.OpeninApp" />

    <style name="OpeninApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_openinapp</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.OpeninApp</item>
    </style>
</resources>